---
layout: posts
title: Projects
description: Some highlighted projects from my past as a web developer
permalink: /projects/index.html
pagination:
  data: collections.project
  size: 20
---

<ul class="index">
  {%- for post in pagination.items -%}
    <li class="h-entry">
      {% if post.data.teaser %}
      <p>
      {% endif %} 
        <time class="dt-published" datetime="{{ post.data.published | asDate | dateToRfc3339 }}">{{ post.data.published | asDate | isoDate }}</time> <a href="{{ post.url | url | relative }}" class="u-url"><span class="p-name">{{ post.data.title }}</span> ({{ post.data.timeframe }})</a>
        <span class="hidden"><a href="{{ site.author.profile }}" class="p-author h-card">{{ site.author.name }}</a></span>
      {% if post.data.teaser %}
      </p>
      {% endif %}
      {% if post.data.teaser %}
      <blockquote>{{ post.data.teaser }}</blockquote>
      {% endif %} 
    </li>
  {%- endfor -%}
</ul>
